---
- name: Validate required variables are defined
  ansible.builtin.assert:
    that:
      - static_website_user_name is defined
      - static_website_user_home is defined
    fail_msg: "Required variable is not defined correctly for {{ inventory_hostname }}"
    quiet: true
  tags:
    - validate

- name: Create static_website user
  ansible.builtin.include_role:
    name: common
    tasks_from: create-service-user.yaml
  tags: always
  vars:
    common_user_name: "{{ static_website_user_name }}"
    common_user_home: "{{ static_website_user_home }}"
