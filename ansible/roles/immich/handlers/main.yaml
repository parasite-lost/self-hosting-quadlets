---
- name: Restart immich
  become: true
  become_user: "{{ immich_user_name }}"
  ansible.builtin.systemd_service:
    service: "immich-pod.service"
    state: restarted
    scope: user
    daemon_reload: true
