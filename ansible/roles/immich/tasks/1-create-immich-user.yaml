---
- name: Create immich user
  ansible.builtin.include_role:
    name: common
    tasks_from: create-service-user.yaml
  tags: always
  vars:
    common_user_name: "{{ immich_user_name }}"
    common_user_home: "{{ immich_user_home }}"
    common_with_systemd_units: false
