---
- name: Validate required variables are defined
  ansible.builtin.assert:
    that:
      - ddns_user_name is defined
      - ddns_user_home is defined
    fail_msg: "Required variable is not defined correctly for {{ inventory_hostname }}"
    quiet: true
  tags:
    - validate

- name: Create ddns user
  ansible.builtin.include_role:
    name: common
    tasks_from: create-service-user.yaml
  tags: always
  vars:
    common_user_name: "{{ ddns_user_name }}"
    common_user_home: "{{ ddns_user_home }}"
    common_quadlets: false
