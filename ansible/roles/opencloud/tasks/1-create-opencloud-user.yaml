---
- name: Validate required variables are defined
  ansible.builtin.assert:
    that:
      - opencloud_user_name is defined
      - opencloud_user_home is defined
    fail_msg: "Required variable is not defined correctly for {{ inventory_hostname }}"
    quiet: true
  tags:
    - validate

- name: Create opencloud user
  ansible.builtin.include_role:
    name: common
    tasks_from: create-service-user.yaml
  tags: always
  vars:
    common_user_name: "{{ opencloud_user_name }}"
    common_user_home: "{{ opencloud_user_home }}"
    common_systemd: false
